{
  "name": "alex-imprimi-2026-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root package for Render deployment",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "start": "npm run start -w server",
    "dev": "npm run dev -w server",
    "check": "npm run check -w server"
  }
}
